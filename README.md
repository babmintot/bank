# Итоговое задание по дисциплине «Тестирование прикладного ПО»

## Описание проекта

В рамках задания выполнено тестирование веб-сервиса перевода денежных средств с банковского счёта на карту.

Реализованы:
- ручное тестирование;
- составление тестовой документации;
- автоматизация тестирования с использованием Selenium;
- настройка CI через GitHub Actions.

---

## Структура репозитория

manual/
├── [test-design.md](manual/test-design.md) — тест-дизайн  
├── [test-cases.md](manual/test-cases.md) — ручные тест-кейсы  
└── [bug-reports.md](manual/bug-reports.md) — баг-репорты  

autotests/
├── tests/ — Selenium тесты  
└── package.json — зависимости и скрипты  

.github/workflows/
└── test.yml — CI (GitHub Actions)  

dist/ — тестируемое приложение  


##  Ручное тестирование

Проведено тестирование следующих сценариев:

- успешный перевод средств;
- отказ при недостатке средств;
- проверка граничного значения суммы;
- валидация номера карты (длина < 16, > 16);
- валидация суммы (отрицательная, пустая);
- проверка расчёта комиссии.

Найденные дефекты:
1. Ошибка обработки граничного значения суммы;
2. Некорректная валидация номера карты (длина > 16);
3. Отсутствие валидации отрицательной суммы.

---

##  Автоматизированное тестирование

Реализованы Selenium-тесты для следующих сценариев:

Позитивные сценарии:
- успешный перевод;
- недостаточно средств;
- отрицательная сумма;
- пустая сумма;
- проверка длины карты (< 16);
- проверка комиссии.

Дефектные сценарии:
- граничное значение суммы (BUG);
- номер карты > 16 цифр (BUG).

---

## Локальный запуск проекта

### Требования

Для запуска необходимо установить:
- Node.js (версия 18+)
- npm
- Python 3
- Google Chrome (последняя версия)

---

## Запуск тестируемого сервиса

cd dist  
python3 -m http.server 8000  

Открыть в браузере:  
http://localhost:8000/?balance=30000&reserved=20001  

---

## Запуск автотестов

Установка зависимостей:

cd autotests  
npm install  

---

Запуск всех тестов:

npm run test-all-local  

Запускаются:
- позитивные тесты  
- дефектные тесты  

---

Запуск только позитивных тестов:

npm run test-positive  

---

Запуск отдельных тестов:

npm run test-success  
npm run test-defect-boundary  
npm run test-defect-card  

---

## CI (GitHub Actions)

Настроен автоматический запуск тестов при:
- push  
- pull request  

CI выполняет:
1. установку зависимостей  
2. запуск тестируемого сервиса  
3. запуск Selenium-тестов  

Особенность:  
Итоговая сборка является КРАСНОЙ (FAILED) намеренно,  
так как автотесты воспроизводят найденные дефекты:

- ошибка граничного значения суммы  
- некорректная валидация номера карты  
